[phases.setup]
aptPkgs = ["libgl1", "libglib2.0-0", "libxcb1", "libxext6", "libsm6", "libxrender1"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip uninstall -y opencv-python",
  "pip install opencv-python-headless==5.0.0.93 --force-reinstall --no-deps"
]